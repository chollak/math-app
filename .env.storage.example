# ===========================================
# FILE STORAGE CONFIGURATION
# ===========================================

# --- Local Storage ---
NODE_ENV=development
STORAGE_BASE_PATH=./database
# Путь к базе данных и файлам

# --- Cloud Storage (Optional) ---
CLOUD_STORAGE_ENABLED=false
# Включить облачное хранилище (true/false)

CLOUD_STORAGE_PROVIDER=r2
# Провайдер: 's3', 'r2', 'spaces', 'b2'

# --- AWS S3 Configuration ---
# Используется для AWS S3, Cloudflare R2, DigitalOcean Spaces
S3_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
# Для AWS S3 оставьте пустым
# Для Cloudflare R2: https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
# Для DO Spaces: https://nyc3.digitaloceanspaces.com

S3_REGION=auto
# AWS: us-east-1, eu-west-1, etc.
# Cloudflare R2: auto
# DO Spaces: nyc3, sfo3, etc.

S3_BUCKET_NAME=ent-app-images
# Имя bucket/container

S3_ACCESS_KEY_ID=your_access_key_here
# Access Key ID

S3_SECRET_ACCESS_KEY=your_secret_access_key_here
# Secret Access Key

# --- CDN Configuration (Optional) ---
CDN_URL=https://cdn.yourdomain.com
# Кастомный CDN URL (опционально)
# Если не указан, используется стандартный S3 URL

# --- Image Processing Settings ---
IMAGE_MAX_FILE_SIZE=10485760
# Максимальный размер файла в байтах (10MB = 10 * 1024 * 1024)

IMAGE_MAX_WIDTH=4000
# Максимальная ширина изображения в пикселях

IMAGE_MAX_HEIGHT=4000
# Максимальная высота изображения в пикселях

IMAGE_THUMBNAIL_SIZE=200
# Размер миниатюры (200x200px)

IMAGE_MEDIUM_WIDTH=800
# Ширина среднего размера

IMAGE_MEDIUM_HEIGHT=600
# Высота среднего размера

IMAGE_LARGE_WIDTH=1920
# Ширина большого размера

IMAGE_LARGE_HEIGHT=1080
# Высота большого размера

IMAGE_QUALITY=85
# Качество WebP сжатия (0-100)

IMAGE_ORIGINAL_QUALITY=90
# Качество JPEG для оригинала (0-100)

# --- Cleanup Settings ---
TEMP_FILE_CLEANUP_ENABLED=true
# Автоматическая очистка временных файлов

TEMP_FILE_MAX_AGE=3600
# Максимальный возраст временных файлов в секундах (1 час)

# ===========================================
# CLOUDFLARE R2 SETUP GUIDE
# ===========================================
#
# 1. Зайдите в Cloudflare Dashboard
# 2. R2 → Create bucket → "ent-app-images"
# 3. Settings → Allow Public Access
# 4. R2 → Manage R2 API Tokens → Create API Token
# 5. Permissions: Object Read & Write
# 6. Скопируйте Access Key ID и Secret Access Key
# 7. Endpoint: https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
#    (найдите в R2 dashboard)
#
# Стоимость R2:
# - Storage: $0.015/GB/month
# - Class A operations (write): $4.50 per million
# - Class B operations (read): $0.36 per million
# - Egress: FREE! 🎉
#
# ===========================================

# ===========================================
# AWS S3 SETUP GUIDE (Alternative)
# ===========================================
#
# 1. Зайдите в AWS Console
# 2. S3 → Create bucket → "ent-app-images"
# 3. Region: выберите ближайший к пользователям
# 4. Bucket settings:
#    - Block all public access: OFF (если нужен публичный доступ)
#    - Versioning: Enabled (рекомендуется)
# 5. IAM → Users → Create User → "ent-app-upload"
# 6. Attach policy:
#    {
#      "Version": "2012-10-17",
#      "Statement": [
#        {
#          "Effect": "Allow",
#          "Action": ["s3:PutObject", "s3:GetObject", "s3:DeleteObject"],
#          "Resource": "arn:aws:s3:::ent-app-images/*"
#        }
#      ]
#    }
# 7. Security credentials → Create access key
# 8. Скопируйте Access Key ID и Secret Access Key
#
# Configuration:
# S3_ENDPOINT=(leave empty for AWS S3)
# S3_REGION=us-east-1
# S3_BUCKET_NAME=ent-app-images
# S3_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
# S3_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
#
# Стоимость AWS S3:
# - Storage: $0.023/GB/month
# - PUT requests: $0.005 per 1,000
# - GET requests: $0.0004 per 1,000
# - Egress: $0.09/GB (первые 10TB)
#
# ===========================================

# ===========================================
# DIGITALOCEAN SPACES SETUP (Alternative)
# ===========================================
#
# 1. DigitalOcean → Spaces → Create Space
# 2. Name: ent-app-images
# 3. Region: NYC3, SFO3, etc.
# 4. Settings → API → Generate New Key
# 5. Скопируйте Key и Secret
#
# Configuration:
# S3_ENDPOINT=https://nyc3.digitaloceanspaces.com
# S3_REGION=nyc3
# S3_BUCKET_NAME=ent-app-images
# S3_ACCESS_KEY_ID=your_key
# S3_SECRET_ACCESS_KEY=your_secret
#
# Стоимость DO Spaces:
# - $5/month за 250GB хранения + 1TB egress
# - Дополнительно: $0.02/GB хранения, $0.01/GB egress
#
# ===========================================
